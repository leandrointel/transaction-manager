spring:
  application:
    name: transaction-manager

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.transactionmanager: DEBUG
